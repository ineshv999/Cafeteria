$kind: http-request
url: "{{base_url}}/cocina/menu/productos"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token_cocina}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Menu visible para cocina', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 5000
