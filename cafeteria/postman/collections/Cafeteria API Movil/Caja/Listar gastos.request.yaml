$kind: http-request
url: "{{base_url}}/caja/gastos"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token_caja}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Gastos visibles para caja', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4400
