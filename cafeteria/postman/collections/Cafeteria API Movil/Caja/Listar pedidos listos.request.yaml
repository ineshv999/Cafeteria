$kind: http-request
url: "{{base_url}}/caja/pedidos"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token_caja}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Pedidos listos visibles para caja', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
