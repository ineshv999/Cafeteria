$kind: http-request
url: "{{base_url}}/detalle-pedido/pedido/{{id_pedido}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token_mesero}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Detalle visible para mesero', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 5000
